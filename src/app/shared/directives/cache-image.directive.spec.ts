import { CacheImageDirective } from './cache-image.directive';

describe('CacheImageDirective', () => {
  it('should create an instance', () => {
    const directive = new CacheImageDirective();
    expect(directive).toBeTruthy();
  });
});
